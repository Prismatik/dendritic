const deref = require('json-schema-deref-sync');
const schemas = require('./schemas');

const derefedSchema = deref(schemas);
delete derefedSchema.definitions;

Object.assign(module.exports, derefedSchema);
