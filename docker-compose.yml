version: '2'
services:
  redbeard:
    build: .
    environment:
      RETHINK_HOST: rethinkdb
      SHELL: /bin/sh
    volumes:
      - .:/opt/app/hostapp
    depends_on:
      - rethinkdb
    links:
      - rethinkdb
  rethinkdb:
    image: rethinkdb
